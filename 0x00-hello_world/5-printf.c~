#include <stdio.h>
/**
 * main - print "with proper grammar, but the outcome is a piece of art," to the console.
 * Return: returns 0 to indicate successful execution.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
