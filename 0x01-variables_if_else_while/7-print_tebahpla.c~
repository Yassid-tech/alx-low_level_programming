#include <stdio.h>

/**
 * main - The lowercase alphabet in reverse.
 *
 * Return: 0 if it's successful.
 */
int main(void)
{
char letter;
for (letter = 'z'; letter >= 'a'; letter--)
putchar(letter);
putchar('\n');
return (0);
}
